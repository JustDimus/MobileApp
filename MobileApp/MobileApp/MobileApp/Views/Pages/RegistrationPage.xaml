<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.Views.Pages.RegistrationPage"
             xmlns:local="clr-namespace:MobileApp.Views.Pages"
             BindingContext="{Binding RegistrationPageViewModel, Source={StaticResource Locator}}"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical"
                     Spacing="15"
                     HorizontalOptions="CenterAndExpand"
                     VerticalOptions="Center">
            <Label Text="{Binding HeaderText}"
                   HorizontalOptions="CenterAndExpand"
                   FontSize="35"
                   TextColor="Black" />
            <Entry Placeholder="{Binding LoginTemplateText}"
                   Text="{Binding LoginProperty, Mode=TwoWay}" />
            <Entry Text="{Binding PasswordProperty, Mode=TwoWay}"
                   IsPassword="True"
                   Placeholder="Password" />
            <StackLayout Orientation="Horizontal">
                <Button Command="{Binding RegistrationCommand}" Text="{Binding RegistrationButtonText}"/>
                <Button Command="{Binding GoToLoginCommand}" Text="{Binding LoginButtonText}"/>
            </StackLayout>
            <Label Text="{Binding ErrorText}"
                   HorizontalTextAlignment="Center"
                   IsVisible="{Binding ShowError}"
                   IsEnabled="{Binding ShowError}"/>
        </StackLayout>
    </ContentPage.Content>
</local:BasePage>